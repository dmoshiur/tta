# Contributing

Create focused changes, preserve API compatibility, validate all input, enforce permissions server-side, and never add fabricated data. Run `npm run typecheck`, `npm test`, and `npm run build`. Test keyboard navigation, reduced motion, 320/360/390/414/768/1024/1440 widths, and both empty and populated states. Document only shipped behavior.
